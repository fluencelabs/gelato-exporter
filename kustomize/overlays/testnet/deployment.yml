apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluence-network-exporter
spec:
  template:
    spec:
      containers:
        - name: fluence-network-exporter
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "50m"
              memory: "160Mi"
